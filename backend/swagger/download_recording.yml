tags:
  - name: Download Recordings
    description: Download individual or bulk audio recordings

paths:
  /api/v1/download_recording:
    get:
      tags:
        - Download Recordings
      summary: Download a single recording
      description: Downloads a specified audio file from the database
      parameters:
        - name: body
          in: body
          required: true
          schema:
            type: object
      responses:
        "200":
          description: Audio file downloaded successfully
          content:
            audio/wav: {}
